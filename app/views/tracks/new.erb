<% if @track && @track.errors.any? %>
  <div>
    <strong> Some issues with your track, please fix them:</strong>
    <ul>
      <% @track.errors.full_messages.each do |track| %>
        <li><%= track %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="add_track">
  <form method="POST" action="/tracks">
    <div class="add">  
      <h3>Add a song to the Music Wall:</h3>
    </div>
    <div>
      <label>Title:</label>
      <input class="input" type="text" name="title" value="<%= @track && @track.title %>">
    </div>
    <div>
      <label>Artist:</label>
      <input class="input" type="text" name="artist" value="<%= @track && @track.artist %>">
    </div>
    <div>
      <label>URL:</label>
      <input class="input" type="text" name="url" value="<%= @track && @track.url %>">
    </div>
    <div>
      <input type="submit" value="Submit">
    </div>
  </form>
</div>